{
  "name": "learning-domain",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "learning/domain/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/learning/domain",
        "main": "learning/domain/src/index.ts",
        "tsConfig": "learning/domain/tsconfig.lib.json",
        "assets": ["learning/domain/*.md"]
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "learning/domain/**/*.ts",
          "learning/domain/package.json"
        ]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "learning/domain/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    }
  },
  "tags": ["", "type:domain", "scope:learning", "side:both"]
}
